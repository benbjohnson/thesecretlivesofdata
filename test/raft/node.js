define(["../../js/raft/node"], function(Node) {
    var assert = chai.assert,
        node   = null;

    describe('Node', function(){
        describe('Node()', function(){
            it('should initialize', function(){
                assert.equal(1, 1);
            });
        });
    });
});
